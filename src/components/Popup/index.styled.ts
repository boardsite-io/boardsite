import styled from "styled-components"

export const PopupCover = styled.div`
    z-index: var(--zIndexPopupBG);
    position: fixed;
    top: -100vh;
    left: -100vw;
    height: 200vh;
    width: 200vw;
`
